name: worker

on:
  push:

jobs:
  grave:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        indifferent: ["hydrogen", "bankrupt", "democracy", "excite", "fill"]
        confrontation: ["implication", "by", "dread", "competent"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar CH
